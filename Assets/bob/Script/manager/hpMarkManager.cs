using UnityEngine;

public class hpMarkManager : MonoBehaviour
{
    public int thisHp;
    public bool futtobasi;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
